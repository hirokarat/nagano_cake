<h1>Admin::Items#index</h1>
<h1>商品一覧</h1>

<% @items.each do |item| %>

<p>id:<%= item.id%></p>

<p>名前:<%= link_to item.name,admin_item_path(item) %></p>

<p>ジャンル:<%= item.genre.name%></p>

<p>価格:<%= item.price%></

<p>ステータス</p>
<%= item.is_active ? '販売中' : '販売停止中'%>

<%end%>
