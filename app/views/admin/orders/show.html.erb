<h1>Admin::Orders#show</h1>
<h1>注文履歴詳細</h1>
<h2>Order</h2>
<p>購入者:<%= @order.customer.name%><%= @order.customer.first_name%></p>

<p>注文日:<%= @order.created_at.strftime('%Y/%m/%d')%></p>

<p>
  配送先:<%= @order.shipping_postal_code%>
          <%= @order.shipping_address%>
</p>
<%= @order.customer.name%><%= @order.customer.first_name%>

<p>支払い方法</p>

<!--商品名など-->
<h2>Order_detail</h2>
<% @order_details.each do |order_detail| %>
<p>
<%= order_detail.item.name%>
:
<%= order_detail.purchase_price%>
:
<%= order_detail.amount%>
:
<%= order_detail.subtotal%>
</p>
<%end%>

<h2>商品合計額</h2>

<p>商品合計:<%= @order.billing_amount%></p>
<p>送料:<%= @order.postage%></p>
<p>請求額合計:<%= @order.billing_amount+@order.postage%></p>
